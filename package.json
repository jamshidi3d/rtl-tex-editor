{
  "name": "rtl-tex-editor",
  "version": "0.1.0",
  "private": true,
  "description": "Local web editor for mixed RTL/LTR TeX, with a live pdf.js preview and two-way SyncTeX.",
  "bin": { "rtl-tex-editor": "server.js" },
  "scripts": {
    "start": "node server.js",
    "thesis": "node server.js --root .. --engine xelatex"
  },
  "engines": { "node": ">=18" }
}
